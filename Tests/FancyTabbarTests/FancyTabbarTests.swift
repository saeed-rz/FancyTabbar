import XCTest
@testable import FancyTabbar

final class FancyTabbarTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(FancyTabbar().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
